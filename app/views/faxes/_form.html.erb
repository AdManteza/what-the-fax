<%= form_with(model: fax, local: true) do |form| %>
  <% if fax.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fax.errors.count, "error") %> prohibited this fax from being saved:</h2>

      <ul>
        <% fax.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
